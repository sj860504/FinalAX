# 계약 변경 노트

- 계약(`../api-contract.md`)이 바뀔 때마다 여기에 `<slug>.<YYYYMMDDTHHMMSS>.md` 한 개. 파일명 끝의 ts 가 순서다.
- `.applied-frontend`, `.applied-backend` = 각 쪽이 **마지막으로 적용한 ts**. 자기 마커만 고친다.
- 새 노트 확인: `/contract-sync` (마커 이후 ts 만 보여준다). 제안: `/contract-change`.
- 이 폴더의 마커·체크박스 갱신은 상대 승인 없이 자기 PR 에 넣는다.
